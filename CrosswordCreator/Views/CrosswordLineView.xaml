<UserControl x:Class="CrosswordCreator.Views.CrosswordLineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CrosswordCreator.Views"
             xmlns:viewModels="clr-namespace:CrosswordCreator.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="30"
             d:DataContext="{d:DesignInstance viewModels:CrosswordLineViewModel}">
    <UserControl.Resources>
        <Style TargetType="{x:Type Label}">
            <Setter Property="BorderBrush" Value="Black"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0 -1 -1 -1"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>
    <StackPanel x:Name="_container" Background="White" Orientation="Horizontal">
    </StackPanel>
</UserControl>
